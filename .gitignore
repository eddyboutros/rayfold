node_modules/
dist/

# signing keys, tokens and passwords for releases: never committed (see .secrets/README.md on your machine)
.secrets/
*.tsbuildinfo
.DS_Store
kotlin/build/
kotlin/.gradle/
bench/results/*.tmp

# every Gradle module build directory, not only the top one
kotlin/**/build/
.kotlin/

# raw downloads: rebuild data/gutenberg.json.gz with `npm run import:gutenberg` (see data/README.md)
data/raw/

# the documentation site: npm run docs:build
docs/.vitepress/dist/
docs/.vitepress/cache/

# builds of the JVM examples
examples/**/build/
examples/**/target/
examples/**/.gradle/

# editor settings
.idea/
*.iml

# Playwright and the proxy check
test-results/
e2e/proxy/cache/

# the JVM member of the fleet check: built by CI, and locally by the command in its README
e2e/fleet/jvm/build/
e2e/fleet/jvm/.gradle/
